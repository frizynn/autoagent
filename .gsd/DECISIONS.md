# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Pipeline mutation scope | Single file (pipeline.py) | Mirrors autoresearch's train.py constraint — prevents search space explosion, keeps diffs reviewable | No |
| D002 | M001 | arch | State storage | Disk-only (.autoagent/ directory) | Crash-recoverable, human-readable, no database dependency — GSD-2 pattern | No |
| D003 | M001 | arch | Archive format | Directory of JSON files + pipeline snapshots | Human-readable, crash-recoverable, git-friendly — one file per iteration | Yes — if performance degrades at scale |
| D004 | M001 | pattern | Meta-agent LLM provider | User's coding agent subscription (Claude Code Max, Codex, etc.) | Provider-agnostic like GSD-2 — system doesn't impose LLM choice | No |
| D005 | M001 | arch | Search strategy autonomy | Meta-agent decides structural vs parameter freely | No explicit phase switching — ADAS-like autonomous decision-making per user preference | No |
| D006 | M001 | arch | Evaluation approach | Multi-metric vector (primary + latency + tokens + cost) | Pareto evaluation prevents reward hacking — user's top concern | No |
| D007 | M001 | pattern | Budget model | Hard dollar ceiling with auto-pause | Simple, predictable — user said "hard ceiling" explicitly | No |
| D008 | M001 | arch | TLA+ verification scope | All pipelines (sequential and concurrent) | User chose universal gate, not concurrency-only — lightweight properties for sequential, full model-checking for concurrent | Yes — if overhead is too high for sequential |
| D009 | M001 | pattern | Data leakage checking | Every evaluation step, not one-time | User's exact words: "ALWAYS CHECK IN EVERY STEP BEFORE RUNNING BENCHMARKS" | No |
| D010 | M001 | scope | Primitive abstraction depth | Thin wrappers with instrumentation | Avoid becoming a framework (R030 anti-feature) — primitives add measurement, not behavior | Yes — if users need richer abstractions |
| D011 | M001/S01 | pattern | MetricsSnapshot immutability | Frozen dataclass | Snapshots are point-in-time records — mutations would break collector aggregation and audit trail | No |
| D012 | M001/S01 | pattern | Cost calculation as standalone function | `calculate_cost()` free function with optional config override | Reusable by any provider without coupling to collector; override dict keeps pricing testable and user-configurable | Yes — if dynamic pricing API needed |
| D013 | M001/S01 | pattern | Protocol aliases (LLM, Retriever) | Module-level aliases for LLMProtocol/RetrieverProtocol | Slice plan expects `from autoagent.primitives import LLM, Retriever` — aliases satisfy that without renaming the Protocol classes | Yes — could rename protocols directly |
| D014 | M001/S01 | pattern | Pipeline module loading via compile()+exec() | Source-level compile instead of importlib.util.spec_from_file_location | importlib's SourceFileLoader uses bytecode cache (.pyc) which returns stale code when the same path is loaded twice in one process — compile()+exec() reads source directly, guaranteeing fresh loads | No |
| D015 | M001/S02 | pattern | Config format | JSON instead of YAML | Boundary map says config.yaml but YAML requires pyyaml dependency. JSON is stdlib-only (`json` module), fully round-trippable, and preserves zero-dependency constraint. TOML has stdlib read (tomllib) but no stdlib write. | Yes — if config complexity demands YAML |
| D016 | M001/S02 | pattern | CLI framework | argparse (stdlib) instead of click/typer | Zero runtime dependencies constraint. argparse handles 3 subcommands fine. | Yes — if CLI grows significantly |
| D017 | M001/S02 | pattern | R006 reinterpretation | Python CLI via argparse, not PI SDK | PI is a Node.js agent harness with no Python SDK. R006 "PI-based CLI" reinterpreted as standard Python CLI with GSD-2-style commands. | No |
| D018 | M001/S03 | pattern | Per-example timeout implementation | ThreadPoolExecutor with shutdown(wait=False, cancel_futures=True) | Context manager's `__exit__` blocks waiting for timed-out threads. Explicit shutdown with wait=False returns immediately after timeout, keeping evaluation wall-clock time proportional to timeout, not pipeline execution time. | No |
| D019 | M001/S03 | pattern | Custom scorer loading | compile()+exec() matching pipeline loader pattern | Consistent with D014 — same dynamic module loading approach for scorers as for pipelines. No caching, fresh load every time. | No |
| D020 | M001/S04 | pattern | EvaluationResult storage format | Dict (not object) in ArchiveEntry JSON | Frozen dataclasses can't be deserialized from JSON directly. Store as dict, reconstruct on demand via evaluation_result_obj property. Keeps JSON files human-readable and self-contained. | Yes — if schema migration is needed |
| D021 | M001/S04 | pattern | Corrupted archive entry handling | Raise ValueError with filename context | Silent skipping would hide data loss. Explicit errors with filename let operators identify and fix corrupted entries. Archive is append-only — corruption is always a bug. | No |
| D022 | M001/S05 | pattern | LLM response source extraction | Pick longest fenced code block | Multiple code blocks in LLM responses are common (explanations + code). Longest block is most likely the complete module. Fallback: treat entire response as source. | Yes — if structured output format used |
| D023 | M001/S05 | pattern | Pipeline source validation depth | Compile + exec into temp module + check run() callable | AST parse alone misses runtime issues (e.g., `run = 42` passes syntax but isn't callable). Exec into temp module catches these. | No |
| D024 | M001/S05 | pattern | First iteration always kept | best_score starts None; any score ≥ None → keep | First successful evaluation must be kept to establish a baseline. Comparing against None naturally handles this. | No |
| D025 | M001/S05 | pattern | MetaAgent failure archive entries | Zero-score EvaluationResult stubs with "proposal_error:" rationale | Archive entries must be structurally complete for query/filter. Stub results keep the schema consistent while marking failures clearly. | No |
| D026 | M001/S06 | pattern | Budget estimation method | Global average cost (total_cost / total_iterations) | More accurate for resumed runs than session-only average — uses full cost history across restarts. | Yes — if iteration costs vary widely |
| D027 | M001/S06 | pattern | Pipeline restoration on resume | Restore from archive's best kept entry, not disk | Disk may have stale/proposed source from mid-iteration crash. Archive is the source of truth for the last known-good pipeline. | No |
| D028 | M002 | arch | Intelligence through prompting, not frameworks | Enrich meta-agent prompt instead of adding DSPy/Optuna dependencies | ADAS finding: free-form code generation with good prompting outperforms hand-crafted search operators. DSPy/Optuna add heavy dependencies and structural assumptions that fight the zero-dependency constraint and existing architecture. Build DSPy-inspired strategies as prompt sections. | No |
| D029 | M002 | arch | Free-form code generation for structural mutations | Meta-agent generates complete pipeline.py source, not AST manipulation | Follows ADAS approach. Component vocabulary + architectural patterns injected as prompt context, but mutations are free-form code — no template systems or formal grammars. Aligns with D001 single-file constraint. | No |
| D030 | M002 | arch | Archive compression via LLM summarization | Use meta-agent's configured LLM for archive summarization, not mechanical compression | Structured summaries (top-K, failure clusters, unexplored regions, trends) preserve more signal than mechanical truncation. Cost tracked in same budget system. Summaries regenerated every N iterations. | Yes — if LLM cost is prohibitive |
| D031 | M002 | pattern | Graduated stagnation signals, not binary mode switching | Sliding-window archive analysis with graduated prompt guidance | Binary explore/exploit switching causes oscillation. Instead: inject graduated signals ("scores plateaued for 8 iterations — consider structural change" vs "recent improvement — consider tuning"). Meta-agent decides freely per D005. | No |
| D032 | M002 | pattern | Strategy selection via prompt signals, not explicit phases | Archive statistics injected into prompt; meta-agent decides mutation type | Per D005 (autonomous strategy decisions), no explicit phase switching. Implementation provides signals (plateau detection, structural diversity, score trends), not commands. | No |
| D033 | M002/S01 | pattern | SummaryResult dataclass instead of raw string | summarize() returns SummaryResult with text, cost_usd, entry_count | Operations with side costs (LLM calls) should return structured results exposing the cost. Same pattern as ProposalResult from M001. Enables callers to track and budget compression costs. | Yes — if summary metadata grows |
| D034 | M002/S01 | pattern | Summary cached on loop instance, not persisted to disk | _cached_summary and _summary_archive_len on OptimizationLoop | Summary is cheap to regenerate (~1 LLM call) and avoids state file complexity. Regeneration triggered by archive growth interval, not time. On crash recovery, summary is simply regenerated. | Yes — if regeneration cost becomes significant |
| D035 | M002/S02 | pattern | Vocabulary built from _PATTERNS list of dicts | Data-driven pattern list instead of string templates | Adding a new architectural pattern is one dict append to `_PATTERNS` — name, description, skeleton. Function generates the full vocabulary text from the list. Easy to extend without touching generation logic. | Yes — if patterns need richer metadata |
| D036 | M002/S03 | pattern | Failed proposals default to mutation_type="parametric" | No real mutation occurred — consistent default | When a proposal fails validation, no code change happened. Tagging as "parametric" avoids inflating structural diversity metrics with non-mutations. | No |
| D037 | M002/S03 | pattern | Strategy detector graceful degradation | Detector failure → empty signals, loop continues | Strategy detection is advisory, not gating. If it raises, the loop logs a WARNING and proceeds with no guidance — meta-agent uses its own judgment. Prevents auxiliary analysis from blocking the optimization loop. | No |
| D038 | M002/S03 | pattern | return/yield excluded from structural patterns | Changing a return value is parametric, not structural | Originally planned as structural indicators. But `return "hello"` → `return "goodbye"` is a parametric change. New returns always accompany new if/def blocks which are caught by other patterns. | No |
| D039 | M002/S04 | pattern | Cold-start detection via exact string match against STARTER_PIPELINE | Exact string comparison, not hash or fuzzy match | Any edit (even whitespace) to the starter template means the user has customized it — cold-start should not overwrite intentional changes. Simple and predictable. | No |
| D040 | M002/S04 | pattern | Separate _build_cold_start_prompt() instead of reusing _build_prompt() | Cold-start has fundamentally different context (no history, no current pipeline) | Reusing _build_prompt() would require many conditional branches. A separate method is clearer and easier to evolve independently. Same section-based construction pattern. | Yes — if prompts converge |
| D041 | M002/S04 | pattern | Benchmark.describe() samples first N examples deterministically | First N, not random sampling | Deterministic output for the same benchmark — makes debugging and testing predictable. Random sampling would make cold-start prompts non-reproducible. | Yes — if representative sampling needed |
| D042 | M003 | arch | Simplicity as Pareto dimension, not separate gate | Complexity metric included in the Pareto vector alongside primary_score, latency, cost | R010 (Pareto) and R020 (simplicity) could conflict as separate gates. Making simplicity a Pareto dimension resolves the conflict: a complex pipeline must dominate on ALL metrics including complexity to be kept. Tiebreaker for incomparable cases: prefer simpler. | Yes — if complexity metric proves unreliable |
| D043 | M003 | arch | Graceful degradation for missing system tools | TLC and Docker: warn + skip when unavailable, don't hard-fail | TLC requires Java, Docker requires Docker daemon. Not all dev environments have both. Safety layers are defense-in-depth — degradation is better than blocking. Same pattern as D037 (strategy detector graceful degradation). | No |
| D044 | M003 | arch | Docker for sandbox, not subprocess restrictions | Docker containers via subprocess, not Python subprocess with restricted permissions | Python subprocess restrictions are too leaky (import escape, signal manipulation). Docker provides proven isolation with well-understood network policy. Not Firecracker (overkill). System-level tool via subprocess — consistent with TLC pattern, not a Python package dependency per D016. | Yes — if Docker proves too heavy |
| D045 | M003 | pattern | Container reuse per iteration, not per example | One Docker container per iteration runs all benchmark examples | Cold-start per example adds 1-5s × N examples overhead. One container per iteration provides sufficient isolation (fresh container per proposed pipeline) while keeping evaluation time proportional to benchmark size, not benchmark size × startup time. | Yes — if isolation requirements tighten |
| D046 | M003 | pattern | Leakage detection: exact match blocks, fuzzy match warns | Hash-based exact example overlap → hard block; n-gram fuzzy overlap → warning only | Exact match (train example in test set) is high-confidence contamination — must block. Fuzzy matching has false positive risk from legitimate shared vocabulary — warning lets the user investigate without blocking valid iterations. | Yes — if false positive rate is acceptable for blocking |
| D047 | M003 | pattern | TLA+ complexity threshold for skip | Skip TLA+ verification for pipelines below complexity threshold (< 10 LOC, no control flow) | Trivial pipelines (single LLM call) don't benefit from formal verification. Generating and checking a spec burns tokens for zero safety value. Archive records "skipped: below complexity threshold" for auditability. | Yes — if threshold needs adjustment |
| D048 | M003 | pattern | Genefication iteration cap | Max 3 attempts for TLA+ spec generation (draft → TLC verify → fix) | Unbounded genefication could burn arbitrary tokens on specs that can't be fixed. 3 attempts balances giving the LLM a fair chance to self-correct against token budget discipline. Failed after 3 → discard proposal. | Yes — if 3 is too few |
| D049 | M003/S02 | pattern | AST-based complexity scoring | ast.walk() node count + weighted branch nodes (×2 for if/for/while/try/with/FunctionDef/AsyncFunctionDef), float('inf') on SyntaxError | Simple, stable, no external deps. Branch weighting reflects control flow complexity. Unparseable = maximally complex ensures bad source always loses tiebreaker. | Yes — if semantic complexity needed |
| D050 | M003/S02 | pattern | Pareto degrades to score-only when metrics are None/zero | candidate_metrics built with 0.0 defaults for missing latency/cost | MockLLM path and any evaluation that doesn't produce full metrics still works — Pareto comparison on score-only dimension naturally reduces to old behavior. No test modifications needed. | No |
| D051 | M003/S04 | pattern | Container-side harness as string constant in sandbox.py | _RUNNER_HARNESS copied via docker cp, not package install in container | Avoids needing autoagent installed inside the container. Harness is self-contained Python that reads JSON stdin, execs pipeline, writes JSON stdout. Must stay in sync with PipelineResult serialization format. | Yes — if harness grows complex |
| D052 | M003/S04 | pattern | Sandbox metadata captured at loop level | sandbox_used, network_policy, fallback_reason — not per-iteration container IDs | Container IDs are internal to SandboxRunner lifecycle. Loop-level metadata captures what matters for post-run inspection: was sandbox used, what network policy applied, why fallback occurred. | No |
| D053 | M004 | arch | Interview output: extended ProjectConfig + context.md | Interview populates ProjectConfig (with new optional fields) for machine consumption, writes context.md for rich meta-agent context | ProjectConfig is the structured contract the loop consumes. context.md captures narrative/domain knowledge that enriches meta-agent prompts without bloating the config schema. Separate concerns: config = what to optimize, context = domain knowledge. | Yes — if a single spec format proves better |
| D054 | M004 | arch | Three slices: interview → benchmark gen → reporting+assembly | S01 interview (high risk), S02 benchmark gen (medium), S03 reporting + end-to-end (low) | Interview is highest risk (LLM quality, spec format). Benchmark gen depends on the spec format S01 establishes. Reporting is low risk and naturally serves as the final assembly proof since it exercises the full flow. Combining reporting with assembly avoids a foundation-only test slice. | No |
| D055 | M004 | pattern | Interview via input()/print(), not TUI framework | Multi-turn LLM conversation using stdlib I/O only | Zero runtime dependencies constraint (D016). Interview UX is conversational, not form-based — input()/print() matches the interaction model. | No |
| D056 | M004 | pattern | Benchmark baseline validation: scores 0.1–0.9 on trivial pipeline | Generated benchmarks must not be trivially solvable or unsolvable | All-0 or all-1 benchmarks provide no optimization signal. Running a baseline pipeline validates the benchmark measures something real. Threshold is conservative — catches degenerate cases without being fragile. | Yes — if thresholds need adjustment |
| D057 | M004/S01 | pattern | Vague-input detection: deterministic rules, not LLM classification | Character length + exact-match phrase list | LLM-based vague detection would add cost and latency to every answer. Deterministic rules (< 10 chars, known vague phrases) are fast, testable, and predictable. LLM is used only for generating follow-up probes, not for classification. | Yes — if detection needs semantic depth |
| D058 | M004/S01 | pattern | SequenceMockLLM as standalone class | Not extending MockLLM | MockLLM has metric-collection coupling (collector, cost_config). SequenceMockLLM is purely about deterministic response sequences for multi-turn testing. Separate class avoids coupling test fixtures to production metrics plumbing. | Yes — if test utilities consolidate |
| D059 | M004/S02 | pattern | Default scoring function "includes" for generated benchmarks | Partial matching is safer than exact for LLM-generated expected values | LLM-generated expected outputs may differ in formatting/whitespace from actual pipeline output. "includes" allows partial matches, reducing false negatives in generated benchmarks. Overridable if user specifies a scorer. | Yes — if exact matching proves better |
| D060 | M004/S02 | pattern | Input diversity threshold at 80% unique | Benchmark with >20% duplicate inputs lacks discriminating power | 80% balances catching degenerate benchmarks (all same input) while allowing legitimate repeated patterns. Below threshold → generation fails with diagnostic. | Yes — if threshold needs adjustment |
| D061 | M004/S02 | pattern | Non-fatal benchmark generation failure in cmd_new | Warning to stderr, config written without benchmark path | User still gets their config from the interview even if benchmark generation fails. cmd_run will catch the missing benchmark path and report it clearly. Avoids losing interview work due to a generation hiccup. | No |
| D062 | M004/S03 | pattern | Report sections as module-level private functions | _score_trajectory, _top_architectures, _cost_breakdown, _recommendations | Composable and independently callable for targeted inspection. No class wrapper needed — functions take archive entries and state as arguments. | Yes — if report customization needed |
| D063 | M004/S03 | pattern | E2e test patches only MockLLM, not MetricsCollector | MetricsCollector must remain real in cmd_run tests | MagicMock instances aren't JSON-serializable. Archive persistence writes collector state to JSON, so MetricsCollector must be real. Only LLM protocol is patched. | No |
| D064 | M005 | arch | Three slices: live dashboard → interview overlay → assembly | S01 streaming dashboard (high risk), S02 interview (medium), S03 report+status+stop+shortcuts (low) | Subprocess streaming is the hardest problem — if spawn+JSONL+overlay doesn't work, nothing else matters. Interview's bidirectional protocol is next riskiest. Assembly slice is all proven patterns (one-shot exec, disk reads, signals). | No |
| D065 | M005 | arch | JSONL output mode for Python CLI, not stdout parsing | Add `--jsonl` flag to cmd_run emitting structured JSON lines per iteration event | Human-readable output is fragile to parse — format changes break the extension. JSON lines are explicit, versioned, and cheap to add as a thin wrapper around existing state writes. | No |
| D066 | M005 | arch | JSON request-response protocol for interview, not PTY forwarding | Python `--json` mode: emit JSON prompt to stdout, read JSON answer from stdin | PTY forwarding would give a terminal-in-terminal experience. JSON protocol lets the TypeScript extension render native pi TUI inputs (SelectList, Input) for each interview phase. More work, better UX. | No |
| D067 | M005 | pattern | Process lifecycle decoupled from overlay lifecycle | SubprocessManager owns the Python process; dashboard overlay is just a view | Closing the overlay must not kill the subprocess. Opening the overlay attaches to a running process. This enables fire-and-forget: start run, close overlay, reopen later. | No |
| D068 | M005 | pattern | PYTHONUNBUFFERED=1 for all spawned subprocesses | Environment variable on spawn, not -u flag | Ensures stdout is line-buffered when piped to Node.js. More reliable than relying on Python's -u flag or flush=True in every print call. | No |
| D069 | M005/S01 | pattern | Loop event_callback instead of JSONL in loop itself | Optional callback parameter on OptimizationLoop, not JSON output hardcoded in loop | Loop stays output-agnostic — CLI layer creates the JSONL callback when --jsonl is active. Keeps loop testable without JSON parsing. Same pattern as primitives_factory. | Yes — if multiple output formats needed |
| D070 | M005/S01 | pattern | SubprocessManager as singleton with event buffer | Single instance, bounded event buffer (200), subscriber pattern | Only one optimization can run at a time (lock file). Singleton matches this constraint. Bounded buffer prevents memory growth in long runs while keeping enough history for dashboard rendering. | Yes — if concurrent runs needed |
